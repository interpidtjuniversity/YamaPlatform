<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clcy.grade_feedback.dao.UserLoginDao">

    <resultMap type="com.clcy.grade_feedback.entity.UserLogin" id="UserLoginMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="studentId" column="studentId" jdbcType="VARCHAR"/>
        <result property="studentName" column="studentName" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryUser" resultMap="UserLoginMap">
        select
            *
        from grade_feedback.student_info
        where studentId = #{userLogin.studentId} and
              password = #{userLogin.password}
        limit 1
    </select>

</mapper>